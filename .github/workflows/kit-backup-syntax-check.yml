name: KIT backup syntax check
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/kit-backup-syntax-check.yml'
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m py_compile kit-backup/sync_kit_backup.py
