name: Probe RED YML exact name
on:
  push:
    branches: [main]
    paths:
      - 'scripts/probe_red_yml_name.py'
      - '.github/workflows/probe-red-yml-name.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests
      - env:
          YANDEX_KIT_TOKEN: ${{ secrets.YANDEX_KIT_TOKEN }}
        run: python scripts/probe_red_yml_name.py
