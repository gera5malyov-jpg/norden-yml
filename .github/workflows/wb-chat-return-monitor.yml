name: WB chat and returns monitor

on:
  push:
    paths:
      - 'scripts/wb_chat_return_monitor.py'
      - '.github/workflows/wb-chat-return-monitor.yml'
  schedule:
    - cron: '17 */3 * * *'
  workflow_dispatch:

concurrency:
  group: wb-chat-return-monitor
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Установить зависимости
        run: pip install requests

      - name: Проверить синтаксис
        run: python -m py_compile scripts/wb_chat_return_monitor.py

      - name: Проверить новые чаты и возвраты Wildberries
        env:
          WB_API_TOKEN: ${{ secrets.WB_API_TOKEN }}
          SMTP_HOST: smtp.yandex.ru
          SMTP_PORT: '465'
          SMTP_USER: shop@office-mag.com
          SMTP_PASSWORD: ${{ secrets.YANDEX_SMTP_APP_PASSWORD }}
          ALERT_EMAIL_TO: shop@office-mag.com
          INITIAL_LOOKBACK_HOURS: '24'
          STATE_FILE: state/wb_chat_return_monitor.json
        run: python scripts/wb_chat_return_monitor.py

      - name: Сохранить курсор и уже обработанные события
        if: success()
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add state/wb_chat_return_monitor.json
          if git diff --cached --quiet; then
            echo "STATE_CHANGED=0"
            exit 0
          fi
          git commit -m "chore: update WB monitor state"
          git pull --rebase origin main
          git push origin HEAD:main
          echo "STATE_CHANGED=1"
