name: Wildberries orders to Webasyst

on:
  push:
    paths:
      - '.github/workflows/wildberries-orders-webasyst.yml'
  schedule:
    - cron: '17 */3 * * *'
  workflow_dispatch:

concurrency:
  group: wildberries-orders-webasyst
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests
      - name: Проверить синтаксис синхронизации
        run: python -m py_compile webasyst/marketplace_order_sync.py
      - name: Перенести заказы Wildberries в Webasyst
        env:
          DRY_RUN: '0'
          CHAT_AUTOMATION_START: '2026-09-23T19:35:00Z'
          SYNC_SOURCES: 'wildberries'
          WB_API_TOKEN: ${{ secrets.WB_API_TOKEN }}
          WEBASYST_API_TOKEN: ${{ secrets.WEBASYST_API_TOKEN }}
          WEBASYST_BASE_URL: https://profikompany.ru
        run: python webasyst/marketplace_order_sync.py
