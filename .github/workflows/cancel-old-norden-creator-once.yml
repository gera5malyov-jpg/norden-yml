name: Cancel slow Norden archive once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cancel-old-norden-creator-once.yml'
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  cancel:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel current chairs/stools update before duplicate audit
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api --method POST repos/${{ github.repository }}/actions/runs/36031235649/cancel || true
