name: Norden — проверить смену категорий Яндекс

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'catalog/verify_yandex_category_changes.py'
      - '.github/workflows/norden-yandex-verify-category-changes.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Установить зависимости
        run: pip install --disable-pip-version-check requests "gspread>=6.1,<7" "google-auth>=2.35,<3"
      - name: Проверить применение категорий
        env:
          YANDEX_MARKET_API_KEY: ${{ secrets.YANDEX_MARKET_API_KEY }}
          YANDEX_MARKET_BUSINESS_ID: '20806099'
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          CATALOG_SPREADSHEET_ID: '1DvMYvKRr8fX8mfLFaeJCXCccQofPViPcIP2CgIctc8w'
          CATALOG_SHEET: 'Норден'
        run: python catalog/verify_yandex_category_changes.py
      - name: Сохранить отчёт
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add catalog/yandex_category_verify_report.json 2>/dev/null || true
          if ! git diff --cached --quiet; then
            git commit -m "Обновить проверку категорий Яндекс"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
