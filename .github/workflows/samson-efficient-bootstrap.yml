name: Samson efficient initial bootstrap

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/samson-efficient-bootstrap.yml'
  workflow_dispatch:
    inputs:
      batch_size:
        description: 'Missing Samson products to create this run'
        required: true
        default: '600'
        type: string

permissions:
  contents: read
  actions: write

concurrency:
  group: samson-kit-write
  cancel-in-progress: true

jobs:
  import:
    runs-on: ubuntu-latest
    timeout-minutes: 345
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Resolve batch size
        id: batch
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "size=${{ inputs.batch_size }}" >> "$GITHUB_OUTPUT"
          else
            echo "size=600" >> "$GITHUB_OUTPUT"
          fi
      - name: Regression tests on first run only
        if: github.event_name == 'push'
        run: PYTHONPATH=samson-kit python -m unittest discover -s samson-kit/tests -v
      - name: Create next missing Samson products
        env:
          SAMSON_API_KEY: ${{ secrets.SAMSON_API_KEY }}
          YANDEX_KIT_TOKEN: ${{ secrets.YANDEX_KIT_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          PYTHONPATH=samson-kit python -m samson_kit.cli \
            --new-only \
            --max-new "${{ steps.batch.outputs.size }}" \
            --report /tmp/samson-bootstrap.json
      - name: Decide whether to continue
        id: next
        if: always()
        shell: bash
        run: |
          python - <<'PY' >> "$GITHUB_OUTPUT"
          import json, os
          path='/tmp/samson-bootstrap.json'
          if not os.path.exists(path):
              print('continue=false')
              raise SystemExit
          r=json.load(open(path,encoding='utf-8'))
          created=int(r.get('new_products_created') or 0)
          errors=int(r.get('error_count') or 0)
          size=int('${{ steps.batch.outputs.size }}')
          ok=(r.get('status')=='ok' and errors==0)
          print('continue=' + ('true' if ok and created>=size else 'false'))
          print('created=' + str(created))
          print('errors=' + str(errors))
          PY
      - name: Queue next Samson missing-only batch
        if: steps.next.outputs.continue == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          BATCH_SIZE: ${{ steps.batch.outputs.size }}
        shell: bash
        run: |
          python - <<'PY'
          import json, os, urllib.request
          repo=os.environ['GITHUB_REPOSITORY']
          url=f"https://api.github.com/repos/{repo}/actions/workflows/samson-efficient-bootstrap.yml/dispatches"
          body=json.dumps({'ref':'main','inputs':{'batch_size':os.environ['BATCH_SIZE']}}).encode()
          req=urllib.request.Request(url,data=body,method='POST',headers={
              'Authorization':'Bearer '+os.environ['GH_TOKEN'],
              'Accept':'application/vnd.github+json',
              'X-GitHub-Api-Version':'2022-11-28',
              'Content-Type':'application/json',
          })
          with urllib.request.urlopen(req,timeout=30) as resp:
              if resp.status != 204:
                  raise RuntimeError(f'dispatch status {resp.status}')
          PY
