# Supplier delta feeds: publish only changed offers. RedBlack/Riva/Everprof keep zero-stock tombstones for 7 days. TD Andrey now switches from the frozen initial full snapshot to changed-only mode.
name: Update supplier delta YML

on:
  schedule:
    - cron: '17 */3 * * *'
  workflow_dispatch:
  push:
    paths:
      - 'sources/process_supplier_deltas.py'
      - 'sources/process_tdandrey.py'
      - 'sources/yml_sources.json'
      - '.github/workflows/supplier-deltas.yml'

permissions:
  contents: write

concurrency:
  group: supplier-deltas
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build changed-only supplier feeds
        run: python sources/process_supplier_deltas.py

      - name: Initialize TD Andrey delta baseline from imported snapshot
        run: cp sources/state/tdandrey_initial.json sources/state/tdandrey.json

      - name: Build TD Andrey changed-only YML
        run: python sources/process_tdandrey.py

      - name: Commit changed feeds and comparison state
        run: |
          git config user.name github-actions
          git config user.email actions@github.com
          git add sources/changed sources/state

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "Update supplier delta YML"
          git pull --rebase origin main
          git push
